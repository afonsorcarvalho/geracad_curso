<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_geracad_curso_pendencias_financeira_aluno_wizard_form" model="ir.ui.view">
        <field name="name">wizard.geracad.curso.pendencias.financeira.aluno</field>
        <field name="model">geracad.curso.pendencias.financeira.aluno.wizard</field>
        <field name="arch" type="xml">
            <form string="Pendências financeiras">
                <sheet>
                    <group>
                       
                           
                            <field name="aluno_id"
                            options="{'no_create_edit': True}"
                             />
                            <field name="matricula_id" domain="domain_matricula_id"
                            options="{'no_create_edit': True}"
                            />

                            <field name="domain_matricula_id" invisible="1"/>
                        
                            
                       
                    </group>
                </sheet>
                <footer>
                    <button string="Confirmar" name="action_confirm" type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

   

   

</odoo>
